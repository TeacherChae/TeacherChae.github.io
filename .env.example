# Supabase 연결 정보 — 복사해서 .env.local 로 만들고 값 채우기:
#   cp .env.example .env.local
# .env.local 은 .gitignore 처리됨(커밋 안 됨). 깃허브 Actions 배포용으로는
# 같은 두 값을 repo Settings → Secrets and variables → Actions 에 등록.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
